# Atlas Trader — Dépendances Python
# Installer avec : pip install -r requirements.txt

# ---- LLM Providers ----
anthropic>=0.40.0
openai>=1.50.0

# ---- Interface Streamlit ----
streamlit>=1.37.0
streamlit-autorefresh>=1.0.1   # F12 : auto-refresh dashboard (30s)
extra-streamlit-components>=0.1.71
plotly>=5.20.0
pandas>=2.2.0

# ---- Authentification (session, cookies, 2FA) ----
bcrypt>=4.1.0            # hachage des mots de passe
PyJWT>=2.8.0             # cookies JWT signés
pyotp>=2.9.0             # TOTP 2FA (Google Authenticator, Authy…)
qrcode[pil]>=7.4.0       # génération du QR code TOTP

# ---- Market Data ----
ccxt>=4.3.0
yfinance>=0.2.40         # forex & commodités (XAU, XAG, WTI, EUR/USD, GBP/USD)

# ---- Web Crawling ----
requests>=2.32.0

# ---- News ----
feedparser>=6.0.11      # RSS
newsapi-python>=0.2.7   # NewsAPI

# ---- Config & Validation ----
pyyaml>=6.0
pydantic>=2.8.0
python-dotenv>=1.0.0

# ---- Stockage ----

# ---- Utilitaires ----
numpy>=1.26.0
scipy>=1.11.0              # V2 quant — deflated Sharpe, stats
scikit-learn>=1.4.0        # V2 quant — LogisticRegression + Platt calibration
lightgbm>=4.3.0            # V2 quant — LightGBM optionnel (use_lgb=true)
hmmlearn>=0.3.2            # HMM pour MarketRegimeAgent + V2 quant.regime
pyarrow>=14.0.0            # V2 quant — cache OHLCV parquet
httpx>=0.27.0
tqdm>=4.66.0
rich>=13.7.0            # logs colorés en CLI

# ---- Notifications ----
python-telegram-bot>=21.0   # optionnel
discord.py>=2.4.0           # optionnel

# ---- MiroFish — Simulation swarm (signal à 40%) ----
# Installé séparément dans le Dockerfile (optionnel — fallback si indisponible)
# pip install git+https://github.com/666ghj/MiroFish.git

# ---- Tests ----
pytest>=8.3.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0
