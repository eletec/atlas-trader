# Atlas Trader — Python dependencies
# Install with: pip install -r requirements.txt
# NOTE: the production image (docker-compose.v4.yml) uses v4/requirements.backend.txt

# ---- Streamlit interface ----
streamlit>=1.37.0
plotly>=5.20.0
pandas>=2.2.0

# ---- Authentication (SQLite sessions, 2FA TOTP) ----
bcrypt>=4.1.0            # password hashing
pyotp>=2.9.0             # TOTP 2FA (Google Authenticator, Authy…)
qrcode[pil]>=7.4.0       # TOTP QR code generation

# ---- Market Data ----
ccxt>=4.3.0

# ---- Web Crawling / HTTP ----
requests>=2.32.0

# ---- Config & Validation ----
pyyaml>=6.0
pydantic>=2.8.0
python-dotenv>=1.0.0

# ---- Storage ----

# ---- Utilities ----
numpy>=1.26.0
httpx>=0.27.0

# ---- Tests ----
pytest>=8.3.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0
