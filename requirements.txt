# Atlas Trader — Dépendances Python
# Installer avec : pip install -r requirements.txt
# NOTE: l'image de production (docker-compose.v4.yml) utilise v4/requirements.backend.txt

# ---- Interface Streamlit ----
streamlit>=1.37.0
plotly>=5.20.0
pandas>=2.2.0

# ---- Authentification (sessions SQLite, 2FA TOTP) ----
bcrypt>=4.1.0            # hachage des mots de passe
pyotp>=2.9.0             # TOTP 2FA (Google Authenticator, Authy…)
qrcode[pil]>=7.4.0       # génération du QR code TOTP

# ---- Market Data ----
ccxt>=4.3.0

# ---- Web Crawling / HTTP ----
requests>=2.32.0

# ---- Config & Validation ----
pyyaml>=6.0
pydantic>=2.8.0
python-dotenv>=1.0.0

# ---- Stockage ----

# ---- Utilitaires ----
numpy>=1.26.0
httpx>=0.27.0

# ---- Tests ----
pytest>=8.3.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0
