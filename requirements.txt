# Atlas Trader — Python dependencies
# Install with: pip install -r requirements.txt
# NOTE: the production image (docker-compose.v4.yml) uses v4/requirements.backend.txt
#
# Versions are pinned deliberately. Everything here used to carry a `>=`, so two
# installs a week apart could resolve to different code — which is how a
# "pandas 2.x" assumption survived into a pandas 3 image. The pins match what is
# running in production; bump them on purpose, not by accident.

# ---- Streamlit interface ----
streamlit==1.62.0
plotly==7.0.0
pandas==3.0.5

# ---- Authentication (SQLite sessions, 2FA TOTP) ----
bcrypt==5.0.0            # password hashing
pyotp==2.10.0            # TOTP 2FA (Google Authenticator, Authy…)
qrcode[pil]==8.2         # TOTP QR code generation

# ---- Market Data ----
ccxt==4.5.76

# ---- Web Crawling / HTTP ----
requests==2.34.2
httpx==0.28.1

# ---- Config & Validation ----
PyYAML==6.0.3
pydantic==2.13.5
python-dotenv==1.2.3

# ---- Storage ----

# ---- Utilities ----
numpy==2.4.6

# ---- Tests ----
# pytest-asyncio and pytest-mock were listed but never imported by any test.
pytest==9.0.3
