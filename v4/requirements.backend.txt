# Dépendances Python — Backend V4
# Subset des dépendances V3 nécessaires + ajouts V4

# ---- API ----
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-multipart>=0.0.9     # upload de fichiers DAG JSON

# ---- Market Data (V3 wrappé) ----
ccxt>=4.3.0

# ---- Dashboard ----
streamlit>=1.35.0
plotly>=5.20.0
qrcode[pil]>=7.4.0
Pillow>=10.0.0

# ---- Authentification ----
bcrypt>=4.1.0
PyJWT>=2.8.0
pyotp>=2.9.0

# ---- Quant (V3 wrappé) ----
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.5.0
hmmlearn>=0.3.2
xgboost>=2.0.0
optuna>=3.6.0               # V6: Bayesian optimization

# ---- IA / LLM ----
litellm>=1.40.0             # proxy universel (Ollama, OpenAI, Anthropic...)
httpx>=0.27.0               # appels async vers Ollama

# ---- Stockage ----
aiosqlite>=0.20.0
PyYAML>=6.0.1
pyarrow>=14.0.0              # support parquet pour cache OHLCV
