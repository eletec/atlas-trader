# Python dependencies — V4 backend
# Subset of the required V3 dependencies + V4 additions
#
# Pinned to the versions running in atlas-v4-api, so rebuilding the image
# reproduces the runtime instead of re-resolving every `>=` from PyPI.

# ---- API ----
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5

# ---- Market Data (wrapped V3) ----
ccxt==4.5.76
requests==2.34.2            # HTTP (utils/http_session, nodes V7)

# ---- Dashboard ----
streamlit==1.62.0
plotly==7.0.0
qrcode[pil]==8.2

# ---- Authentication ----
bcrypt==5.0.0
pyotp==2.10.0

# ---- Quant ----
numpy==2.4.6
pandas==3.0.5
xgboost==3.2.0

# ---- AI / LLM ----
litellm==1.98.0             # universal proxy (Ollama, OpenAI, Anthropic...)
httpx==0.28.1               # async calls to Ollama

# ---- Storage ----
PyYAML==6.0.3
python-dotenv==1.2.3
